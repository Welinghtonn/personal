import styled from "styled-components";

export const ReturnComponent = styled.div`
    display: flex;
    align-items: center;
    cursor: pointer;

    button{
        cursor: pointer;
        background: transparent;
        font-size: 20px;
        border: none;
    }
`